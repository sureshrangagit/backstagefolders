import * as React from 'react';
import type { Align, Side } from '../../utils/useAnchorPositioning.js';
import { useMenuPositioner } from './useMenuPositioner.js';
import { BaseUIComponentProps } from '../../utils/types.js';
/**
 * Positions the menu popup against the trigger.
 * Renders a `<div>` element.
 *
 * Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)
 */
declare const MenuPositioner: React.ForwardRefExoticComponent<MenuPositioner.Props & React.RefAttributes<HTMLDivElement>>;
declare namespace MenuPositioner {
    interface State {
        /**
         * Whether the menu is currently open.
         */
        open: boolean;
        side: Side;
        align: Align;
        anchorHidden: boolean;
        nested: boolean;
    }
    interface Props extends useMenuPositioner.SharedParameters, BaseUIComponentProps<'div', State> {
    }
}
export { MenuPositioner };
