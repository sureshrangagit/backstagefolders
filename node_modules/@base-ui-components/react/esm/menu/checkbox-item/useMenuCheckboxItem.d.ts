import { useMenuItem } from '../item/useMenuItem.js';
export declare function useMenuCheckboxItem(params: useMenuCheckboxItem.Parameters): useMenuCheckboxItem.ReturnValue;
export declare namespace useMenuCheckboxItem {
    interface Parameters extends useMenuItem.Parameters {
        checked?: boolean;
        defaultChecked?: boolean;
        onCheckedChange?: (checked: boolean, event: Event) => void;
    }
    interface ReturnValue extends useMenuItem.ReturnValue {
        checked: boolean;
    }
}
