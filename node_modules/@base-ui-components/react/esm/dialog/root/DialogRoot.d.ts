import * as React from 'react';
import { type SharedParameters, useDialogRoot } from './useDialogRoot.js';
/**
 * Groups all parts of the dialog.
 * Doesn’t render its own HTML element.
 *
 * Documentation: [Base UI Dialog](https://base-ui.com/react/components/dialog)
 */
declare const DialogRoot: React.FC<DialogRoot.Props>;
declare namespace DialogRoot {
    interface Props extends SharedParameters {
        children?: React.ReactNode;
    }
    type Actions = useDialogRoot.Actions;
}
export { DialogRoot };
