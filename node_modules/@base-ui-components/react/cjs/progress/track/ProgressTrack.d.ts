import * as React from 'react';
import { ProgressRoot } from '../root/ProgressRoot.js';
import { BaseUIComponentProps } from '../../utils/types.js';
/**
 * Contains the progress bar indicator.
 * Renders a `<div>` element.
 *
 * Documentation: [Base UI Progress](https://base-ui.com/react/components/progress)
 */
declare const ProgressTrack: React.ForwardRefExoticComponent<ProgressTrack.Props & React.RefAttributes<HTMLSpanElement>>;
declare namespace ProgressTrack {
    interface State extends ProgressRoot.State {
    }
    interface Props extends BaseUIComponentProps<'div', State> {
    }
}
export { ProgressTrack };
