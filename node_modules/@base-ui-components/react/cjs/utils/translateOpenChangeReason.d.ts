import type { OpenChangeReason as NativeReason } from '@floating-ui/react';
export type OpenChangeReason = 'click' | 'hover' | 'focus' | 'focus-out' | 'escape-key' | 'outside-press' | 'trigger-press';
export declare function translateOpenChangeReason(nativeReason?: NativeReason): OpenChangeReason | undefined;
