import { useMenuItem } from '../item/useMenuItem.js';
export declare function useMenuRadioItem(params: useMenuRadioItem.Parameters): useMenuRadioItem.ReturnValue;
export declare namespace useMenuRadioItem {
    interface Parameters extends useMenuItem.Parameters {
        checked: boolean;
        setChecked: (event: Event) => void;
    }
    interface ReturnValue extends useMenuItem.ReturnValue {
        checked: boolean;
    }
}
