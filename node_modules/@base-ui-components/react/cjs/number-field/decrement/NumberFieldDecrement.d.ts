import * as React from 'react';
import type { NumberFieldRoot } from '../root/NumberFieldRoot.js';
import { BaseUIComponentProps } from '../../utils/types.js';
/**
 * A stepper button that decreases the field value when clicked.
 * Renders an `<button>` element.
 *
 * Documentation: [Base UI Number Field](https://base-ui.com/react/components/number-field)
 */
declare const NumberFieldDecrement: React.ForwardRefExoticComponent<NumberFieldDecrement.Props & React.RefAttributes<HTMLButtonElement>>;
declare namespace NumberFieldDecrement {
    interface State extends NumberFieldRoot.State {
    }
    interface Props extends BaseUIComponentProps<'button', State> {
    }
}
export { NumberFieldDecrement };
