import { jsxs, jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import clsx from 'clsx';
import { Text } from '../../Text/Text.esm.js';
import { Link } from '../../Link/Link.esm.js';
import { Avatar } from '@base-ui-components/react/avatar';

const TableCellProfile = forwardRef(
  ({ className, src, name, to, withImage = true, ...rest }, ref) => /* @__PURE__ */ jsxs(
    "div",
    {
      ref,
      className: clsx("canon-TableCellProfile", className),
      ...rest,
      children: [
        withImage && /* @__PURE__ */ jsxs(Avatar.Root, { className: "canon-TableCellProfileAvatar", children: [
          /* @__PURE__ */ jsx(
            Avatar.Image,
            {
              src,
              width: "20",
              height: "20",
              className: "canon-TableCellProfileAvatarImage"
            }
          ),
          /* @__PURE__ */ jsx(Avatar.Fallback, { className: "canon-TableCellProfileAvatarFallback", children: (name || "").split(" ").map((word) => word[0]).join("").toLocaleUpperCase("en-US").slice(0, 1) })
        ] }),
        name && to ? /* @__PURE__ */ jsx(Link, { to, children: name }) : /* @__PURE__ */ jsx(Text, { variant: "body", children: name })
      ]
    }
  )
);
TableCellProfile.displayName = "TableCellProfile";

export { TableCellProfile };
//# sourceMappingURL=TableCellProfile.esm.js.map
