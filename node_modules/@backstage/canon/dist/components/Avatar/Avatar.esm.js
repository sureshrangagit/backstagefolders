import { jsxs, jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { Avatar as Avatar$1 } from '@base-ui-components/react/avatar';
import clsx from 'clsx';

const Avatar = forwardRef(({ className, src, name, size = "medium", ...props }, ref) => /* @__PURE__ */ jsxs(
  Avatar$1.Root,
  {
    ref,
    className: clsx("canon-AvatarRoot", className),
    "data-size": size,
    ...props,
    children: [
      /* @__PURE__ */ jsx(Avatar$1.Image, { className: "canon-AvatarImage", src }),
      /* @__PURE__ */ jsx(Avatar$1.Fallback, { className: "canon-AvatarFallback", children: (name || "").split(" ").map((word) => word[0]).join("").toLocaleUpperCase("en-US").slice(0, 2) })
    ]
  }
));
Avatar.displayName = Avatar$1.Root.displayName;

export { Avatar };
//# sourceMappingURL=Avatar.esm.js.map
