import { jsx, jsxs } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { Checkbox as Checkbox$1 } from '@base-ui-components/react/checkbox';
import { Icon } from '@backstage/canon';
import clsx from 'clsx';

const Checkbox = forwardRef(
  (props, ref) => {
    const {
      label,
      checked,
      onChange,
      disabled,
      required,
      className,
      name,
      value,
      style
    } = props;
    const checkboxElement = /* @__PURE__ */ jsx(
      Checkbox$1.Root,
      {
        ref,
        className: clsx("canon-CheckboxRoot", className),
        checked,
        onCheckedChange: onChange,
        disabled,
        required,
        name,
        value,
        style,
        children: /* @__PURE__ */ jsx(Checkbox$1.Indicator, { className: "canon-CheckboxIndicator", children: /* @__PURE__ */ jsx(Icon, { name: "check", size: 12 }) })
      }
    );
    return label ? /* @__PURE__ */ jsxs("label", { className: "canon-CheckboxLabel", children: [
      checkboxElement,
      label
    ] }) : checkboxElement;
  }
);

export { Checkbox };
//# sourceMappingURL=Checkbox.esm.js.map
