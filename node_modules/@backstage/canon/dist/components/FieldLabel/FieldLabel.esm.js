import { jsxs, jsx } from 'react/jsx-runtime';
import { Label } from 'react-aria-components';
import { forwardRef } from 'react';

const FieldLabel = forwardRef(
  (props, ref) => {
    const { label, secondaryLabel, description, htmlFor, id, ...rest } = props;
    if (!label) return null;
    return /* @__PURE__ */ jsxs("div", { className: "canon-FieldLabel", ...rest, ref, children: [
      label && /* @__PURE__ */ jsxs(Label, { className: "canon-FieldLabelLabel", htmlFor, id, children: [
        label,
        secondaryLabel && /* @__PURE__ */ jsxs(
          "span",
          {
            "aria-hidden": "true",
            className: "canon-FieldLabelSecondaryLabel",
            children: [
              "(",
              secondaryLabel,
              ")"
            ]
          }
        )
      ] }),
      description && /* @__PURE__ */ jsx("div", { className: "canon-FieldLabelDescription", children: description })
    ] });
  }
);
FieldLabel.displayName = "FieldLabel";

export { FieldLabel };
//# sourceMappingURL=FieldLabel.esm.js.map
